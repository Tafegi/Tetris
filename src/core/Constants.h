#pragma once

#include "Types.h"

namespace core
{
    inline constexpr i32 BOARD_WIDTH  = 10;
    inline constexpr i32 BOARD_HEIGHT = 20;

    inline constexpr i32 NEXT_QUEUE_SIZE = 5;

    inline constexpr i32 FPS_LIMIT = 60;

    inline constexpr f32 LOCK_DELAY = 0.5f;
    inline constexpr f32 DAS = 0.15f;
    inline constexpr f32 ARR = 0.02f;
}